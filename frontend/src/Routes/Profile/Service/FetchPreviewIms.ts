export interface FileData {
    id: string,
    file: File
    filename: string
}

export const FETCH_IMAGE_COUNT = 8

export default function FetchPreviewIms(offset: number): FileData {
    return {
        
    }
}